<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Privacy guarantee (spec N1): this app declares NO android.permission.INTERNET,
      ever. Only foreground-service and POST_NOTIFICATIONS permissions appear below.
      A static check asserts the merged manifest omits INTERNET.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MP4toMP3converter">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MP4toMP3converter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Drains the conversion queue in the background (spec §6.4). Declares BOTH FGS types;
          the runtime picks mediaProcessing (API >= 35) or dataSync (API 31-34) in code.
        -->
        <service
            android:name=".jobs.ConversionService"
            android:exported="false"
            android:foregroundServiceType="dataSync|mediaProcessing" />

        <receiver
            android:name=".jobs.CancelReceiver"
            android:exported="false" />
    </application>
</manifest>
