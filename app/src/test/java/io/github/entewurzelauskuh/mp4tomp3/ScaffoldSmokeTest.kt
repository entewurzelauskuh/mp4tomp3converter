package io.github.entewurzelauskuh.mp4tomp3

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Trivial JVM unit test proving the `test` source set + task wiring works from a
 * clean checkout. Replaced by real domain tests in Phase 1.
 */
class ScaffoldSmokeTest {
    @Test
    fun arithmeticSanity() {
        assertEquals(4, 2 + 2)
    }
}
